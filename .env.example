# Postgres. Locally this is the docker-compose service; in production point it at a hosted
# instance (Neon, Supabase, Railway). The app and the worker MUST share one database — the job
# queue, the rate limiter and the HTTP cache all live in it.
DATABASE_URL=postgres://viraly:viraly@localhost:5433/viraly
